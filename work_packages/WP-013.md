# WP-013 — Native builds and real-device verification

Dependencies: WP-012.
Read: `docs/16_BUILD_AND_RELEASE.md`. Also read AGENTS.md and the product contract.

## Implement and verify
1. **BUILD-001:** Reproducible clean restore/build/import/export produces Windows and Linux native packages with dependency hashes.
2. **BUILD-002:** Distribution includes player controls/rules/help/notices/known limitations and a candidate manifest without copied franchise assets.
3. **DEVICE-001:** Windows x86_64 native package actually launches and completes a local economic match.
4. **DEVICE-002:** Linux x86_64 native package actually launches and completes a local economic match.
5. **DEVICE-003:** Two actual local controllers and keyboard-plus-controller are assigned and exercised without cross-seat input.
6. **DEVICE-004:** Two physical machines complete a private 1v1 LAN match with matching confirmed result and debit logs.
7. **DEVICE-005:** Actual Windows/Linux deterministic replay comparison yields matching canonical hashes for the same content/inputs.

## Deliverables
Working source/data integrated with the main executable, production tests for the requirements above, reproducible commands, actual logs and required visual/replay/binary evidence. Update reports/STATE.json and acceptance records only after actual verification. Keep content/schema/fixtures consistent. Do not present reference-oracle output as combat evidence.

## Integration and stop rule
Build/run after integration and repair any new regressions. A requirement marked target_device/human requires the actual named device/person; leave it explicitly pending when unavailable and finish unrelated software. Passing this package is not completion of the user's full mission. Continue to the next eligible package until the bounded release/handoff finish. At a true execution boundary, write the precise resume packet.

## Embedded executor instruction
Implement WP-013 in the current repository against its listed source contracts and acceptance ids. Inspect existing work before changes. Prove behavior in the real core/shell, not a mock alone. Do not change the user's 1v1/single-wallet scope or weaken gates. Integrate and continue the complete mission after this package.
