You are GPT6-Astra, implementation lead for Strike Ledger. This directory is the complete authoritative build pack. Build the ENTIRE bounded prototype end to end; do not return another plan or stop after one package.

Read START_HERE.md, AGENTS.md, docs/00_PRODUCT_CONTRACT.md, docs/01_DECISIONS_AND_AUTHORITY.md, docs/02_ARCHITECTURE.md, work_packages/INDEX.md and acceptance/requirements.json. Inspect actual workspace/Git/toolchain before editing; run the scaffold checks. Resolve a genuinely inconsistent source with a documented decision and regression, not a quiet assumption.

Implement an ORIGINAL traditional 2D fighter with Third Strike–inspired grounded six-button combat, motion/charge inputs, high/low/air/red parries, throws/techs, confirms/cancels, knockdown/stun and two complete original fighter kits. The only spendable resource is the player's persistent CREDIT WALLET. Optional preparation leases and every EX/selected-super startup spend from it. No earned super/EX/drive meter, prepaid usage stocks/tickets, attack/defense income or hidden alternative gauge. Ordinary offense/defense is fully usable at zero credits.

EXCLUSIVELY 1v1: two opposing players, one arena. Do not implement teams, transfers, shared wallets, split arenas, relays, assists, four-seat systems or team balance. This explicitly supersedes the previous platform/team concept.

Complete singles preparation→reveal→fight→confirmed settlement→next round/result, bots, training/drills, deterministic replays, private 1v1 rollback, controller assignment/remapping, all UI/audio/original presentation and Windows/Linux native packages. Local first-playable is a milestone, not the final deliverable. Execute every mandatory work package and keep integrating the actual executable.

Use real parallel specialists only where available and appropriate, with one integrator and contract owner. Otherwise execute serially. Safely provision official free per-user prerequisites when possible; do not require Capcom/Nintendo material, paid services, undocumented model commands, unrelated projects or public networking. Preserve owner work; do not publish/push without authorization.

Add production C# conformance/integration tests that execute the real core; the Python oracle is not the game. Verify the exact startup debit, insufficient-funds behavior, rollback spending restoration, atomic preparation and confirmed-only payouts. Run real two-process socket matches and latency/loss fault tests, not only network mocks. Observe actual gameplay, record full match/shop/parry/paid-action evidence and export/launch on available targets.

Continue implementation→build→run→observe→test→repair until every agent-solvable mandatory requirement is complete and evidenced. Never declare completion from documentation, compile success, a reference suite or a status file. Do not edit gates to pass. Actual missing physical hardware/human response is an external gate, not a fabricated pass or reason to abandon other work.

Maintain reports/STATE.json. At context/quota/tool boundaries write a precise reports/RESUME_PACKET.md, with source state, commands/results, failures, evidence, blockers and next executable steps. Do not claim unattended background progress.

Finish with actual artifact paths/launch instructions, requirement status across software/device/human tiers, tested conditions, known limitations and owner feel checklist. Start by inspecting the workspace and running checks, then IMPLEMENT.
