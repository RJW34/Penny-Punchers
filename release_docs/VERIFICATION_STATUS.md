# Verification status

This documentation describes **Shop-Only Economy Rework v2**, layered on the September 2026 audit, Buyables mechanics and After Hours/Marist presentation. It does not itself certify an export.

The packaged `RELEASE_CANDIDATE.json` identifies the actual source/content/build inputs. `docs/UPGRADE_STATUS.json` and `docs/UPGRADE_REVIEW.md` record executed checks and pending gates; source equivalents live under `reports/`. Use those artifacts instead of assuming that files named “final” from an earlier run describe the current build.

Current suites must exercise purchased EX without combat payments, one-use super rollback, confirmed reward eligibility/caps/settlement, atomic mixed carts, replay/network identity and the 100/115-node current catalogs. Actual native shop/HUD/results and all-stage camera evidence are separate from headless tests. A build passing compilation alone is not gameplay verification.

The candidate-specific [presentation review](PRESENTATION_REVIEW.md) records the completed 299-check exported UI run, actual health-bar alignment and Marist/shop captures. It identifies the fresh renamed UI run, completed replacement combat showcase, and Windows/free-kit/network/Linux recordings; interrupted attempts remain separate.

The earlier 76/76 After Hours software result, original 98-action catalog, import-time test counts and direct-spend balance experiment are historical. The v2 changes do not qualify for an unchanged-gameplay compatibility waiver. Exact source/binary hashes bind v2 evidence to its export. The later Thomas/Vincent display-name swap has a separate exact source, mechanical-data and Core-method comparison; earlier v2 replay bytes and identities remain unchanged, with fresh renamed executable/UI checks.

Physical controllers, two separate physical LAN computers and owner/friend feel/balance acceptance remain **NOT_RUN** unless the current ledger contains actual new evidence. No number of injected-input or bot tests supplies those verdicts. See `KNOWN_LIMITATIONS.md` for art and platform limits.
