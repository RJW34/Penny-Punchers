# Authority and resolved decisions
Newest explicit user instructions: integrate Shop-Only Economy Rework v2 into the Penny-Punchers upgrade, preserving the expanded audit and implemented Buyables v1 content where compatible. The active resource contract is `docs/21_SHOP_ONLY_V2.md`. Traditional fighter / Third Strike feel, strictly1v1 and no teammate economy remain. The user also requested Thomas/Vincent names, the32-bit graphics, both Marist stages and the Penny-Punchers Git repository.

Authority order: this product contract and these explicit instructions; system semantics in docs/; canonical numeric/character/item data in data/; acceptance requirements; work packages; research/context; starter code and reference oracle. The oracle implements proposed arithmetic, not a substitute for the game or permission to contradict semantics. Resolve genuine conflicts explicitly with a decision record and regression test.

Approved design defaults: Godot .NET presentation plus deterministic C# simulation; two original fighters; six digital attack buttons; free high/low/air/red parry; scalar bank frozen in combat; round shop with three optional rentals, up to two repeatable EX licenses and one optional single-use super; bounded nonspendable skill receipts deposited at confirmed settlement; public revealed kits; first-to-five points within nine rounds; local and private rollback. No additional approval is needed for the requested integration.

Every number is an experimental original seed. The agent may fix provably inconsistent data, or tune through recorded tests, while preserving the user's scope and the core resource identity. Record old/new values, rationale, fixture changes, content hash, balance consequences and evidence. Do not weaken required capabilities, change the monetary model or waive a gate merely to finish.

## Architecture choice
We retain the independent C# core rather than adopting generic rigid-body character motion. A custom state machine makes action clocks, motion recognition, collision, input latency, transaction rollback and replays explicit. Godot is the shell, not a promise of Street Fighter feel. Specialized fighting engines are possible alternatives, but a late engine rewrite is not authorized solely to avoid unfinished work. Reuse a vetted low-level library only when it demonstrably meets the contract and its license/dependency is recorded.

## No old-repository dependency
This is self-contained. Use the user-requested source and asset packages and the named Penny-Punchers repository. Do not import unrelated private projects or perform unrelated cleanup, expenses, admin changes or router exposure. Publishing source to the requested private Git repository is within the user's explicit repository request; preserve source provenance and keep unrelated private material out.
