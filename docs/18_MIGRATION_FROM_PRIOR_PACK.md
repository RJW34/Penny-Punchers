# Replacement, not overlay
This pack supersedes the prior Split Circuit platform-fighter scaffold and interrupted transitional traditional-fighter drafts. It is strictly 1v1; the user's last instruction overrides the original teammate idea. Start in a NEW folder unless deliberately migrating an existing implementation.

If migrating: inspect Git/worktree and create a reversible branch/backup without deleting owner work; inventory working code/tests/assets; map each kept component to a current requirement; preserve generic utilities only when applicable. Keep original work archived separately, not as active contradictory instructions.

Remove from active architecture/data/schema/protocol/modes/tests: platform stocks/blast zones/ledge systems/air-dodge mechanics; separate arenas, four-player seating, teams/pairings/relays/transfers, donation/provenance buckets, shared scoring and team economy balance. Remove all super/EX earned gauges and precharged paid-use inventory. Do NOT rename those fields and keep their behavior. The new scalar wallet must be part of the same rollback state as the fight.

Port useful independent serialization/input/network tooling only after new conformance tests. Replace content tables, move semantics, resource rules, acceptance inventory, work-package graph and status records. Old “passed” evidence cannot be migrated to the new game; rerun on the new candidate. Do not blindly overwrite an existing project with this zip or claim tests from one game validate the other.
