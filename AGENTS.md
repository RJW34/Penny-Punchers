# Agent operating contract
## Current user-approved economy: Shop-Only Rework v2
The user's integration request for `Penny-Punchers_Shop-Only_Economy_Rework_v2` supersedes earlier direct-spend, always-owned EX, reserve-floor, no-skill-income and free match-locked super rules. The active default must use a round shop: three optional rental slots, up to two repeatable EX licenses, and at most one purchased super with one legal startup. Credits move only at atomic shop commit and confirmed round settlement. CH/AA/precision-parry receipts remain nonspendable until settlement. No combat affordability gate, per-use EX ammunition, reserve floor or automatic execution. Preserve every already implemented move, both Marist stages, and Thomas/Vincent naming. See `docs/21_SHOP_ONLY_V2.md` for the current contract and historical-spec boundaries.

Implement the ENTIRE bounded game, not another scaffold. Latest user constraints: Third Strike–inspired traditional fighting, economy instead of all super/EX meter, strictly 1v1 without teammate systems. No earlier-chat dependence.

Inspect workspace/Git/tools first, preserve existing owner work, follow authority hierarchy, run initial pack checks. Read relevant system docs before editing. One integrator owns shared state/protocol/resource contracts. Parallelize only using actually available tools with non-overlapping ownership. Otherwise work serially without inventing subagent results.

Numeric data is canonical; docs define semantics. Fix conflicts with decisions/tests. Never use Godot default rigid-body physics as a substitute for the prescribed deterministic fighting core. No generic arcade button-mash implementation disguised as Third Strike feel. Credits must replace, not supplement or rename, an earned gauge.

No fake tests, empty implementations passing success, status-only proof, staged screenshots, speculative-payout exploits or waived requirements. Compilation is not gameplay. Initial reference tests are not production combat tests. Device/human gates may be honestly pending; unrelated work must continue.

Work through every mandatory work package, continually integrate→build→run→observe→test→repair. Do not stop after local movement, first playable, first package or passing unit tests. No paid services, copied franchise assets/ROMs, unrelated user repo imports, admin changes, remote publishing or router exposure. Maintain accurate STATE/RESUME_PACKET and final artifact/evidence paths.
