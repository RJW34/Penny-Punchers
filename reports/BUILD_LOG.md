# Shop-only v2 verification in progress

The historical After Hours result is preserved in `history/PRE_SHOP_V2_BUILD_LOG.md`. It does not certify the changed game.

Current v2 candidate exports and native evidence are being rebuilt. Consult `RELEASE_CANDIDATE.json`, `ACCEPTANCE_RESULTS.json`, and the current `shop-v2` evidence. No full acceptance result is claimed until this release is bound to those artifacts.
