# AFTER HOURS integration verification

The supplied art is integrated into native Strike Ledger candidate `source-sha256:5414744b92e51de04d4be67df1929929a1072e7d5b986d773b1459166217784d`, created at 2026-09-05 11:10:24 UTC. This report separates completed current presentation checks from the preserved fighting-game verification. New native movies, fresh package runs, substantive and visual reviews, and the full 51-check compatibility audit are complete. The unchanged software gate passed 76/76 against the current evidence proposal; all-tier verification records 79/82 with only DEVICE-003, DEVICE-004 and HUMAN-001 NOT_RUN.

## Completed current-candidate observations

The actual identity-only audit passed all 18 checks, confirming unchanged Core/App source inputs and all 13 canonical JSON files, exact prior Core/App binaries on both exported platforms, fingerprinted art and matching current export inventories. The current Windows shell DLL is `e19abeacdf69aa0dce26161e2f0589e6f7c4c49f550eb559b8e4c66b511107db`; its PCK is `471731809ef120516c33cef6171cf1c8e289e8695277f47b4fa64f7befbc5b6b`. The final UI, art and normal-pacing process records each bind all five runtime files to the candidate. `reports/evidence/after-hours-compatibility/identity-only.json` records compatibility, not full acceptance.

The exported GUI passed 75/75 actual software-injected controller-event checks, including the completed replay/result/rematch flow, with 11 screenshots and a clean process exit. Its fully decoded 60 FPS movie contains 1,003 frames over 16.716667 seconds; SHA-256 is `3b6a7d77de9707a9bb52c403427c4415a18af73a2a484609b9950771c5a0f2bb`. See `reports/evidence/native-after-hours-ui/`. These events do not certify physical controller hardware or settings persistence; the original separate native settings test remains protected by unchanged source/binary compatibility.

The native art fixture passed all 415 selections: 290 nonempty canonical move phases and 125 state keys across both fighters, with both mirror palettes and 38 screenshots. These are actual renderer fixtures, not legal-input matches. The separate visual review directly inspected all 49 final UI/art PNGs and found them consistent with the reviewed runtime images; see `reports/evidence/native-after-hours-art/` and `reports/evidence/after-hours-visual-review.json`. Source boundary review passed 23 checks, with four presentation defects resolved: authoritative reactions now precede cosmetic cues, parry and taunt retain their poses through global freeze, and frozen backdash preserves its direction cue. That review distinguishes read source semantics from executed identity checks and records the reviewer's involvement; see `reports/evidence/after-hours-boundary-review.json`.

The normal Windows pacing run completed six rounds and 12,789 simulation ticks without movie capture or screenshot writes, with clean exit 0 and the same verified final hash `c4d779b7d9ef1fbfa9b378e4b18fe422169b4b79b1b89d441492cf01e169c51d`. The i7-7700HQ/GTX 1050 Ti run measured 13,665 frame intervals: median 16.6657ms, p95 16.8021ms, p99 17.3155ms, worst 557.2761ms. Of 228 logged FPS samples, 224 read 60; the four others were 29, 34, 50 and 57. Measured elapsed time was 230.320993 seconds, or 232.750 seconds including process launch/shutdown. These callback timings establish mostly consistent pacing with isolated stalls, not perfect 60 FPS or physical input latency. The scoped CPU callback measurements exclude GPU work after submission. Actual reports/logs are under `reports/evidence/native-after-hours-pacing/`.

The final legal-input showcase passed all 13 assertions with 14 screenshots and a fully decoded 57.000-second movie; its SHA-256 is `547171274ba0b713b314d90ceb803dbb153992d8cd22d0211f2488c0139b4a15`. Both final graphical Windows/Linux matches completed all six rounds and 12,789 ticks with clean exits, identical canonical result `c4d779b7d9ef1fbfa9b378e4b18fe422169b4b79b1b89d441492cf01e169c51d`, and byte-identical full competitive replay SHA-256 `371c2be65f805c377938b6eb74b551ceb2e3cc97a632b41f8d52fe47e9baac46`. Actual movie validation has also completed: Windows 229.000 seconds, SHA-256 `434f12013e90be401110f78dbbad79f82dd409f5e59f2a4a14f7502d2a511427`; Linux 228.866667 seconds, SHA-256 `5e480c2ac316e6c251a107066f46afbb10d7d7cc125d217a1283bb3ad6c17ac2`. Each full-match movie has 54 validated screenshots, a complete decode and a matching actual MP4 hash. These captures establish visible native play; fixed movie FPS does not measure normal pacing. Linux remains scoped to this PC's existing WSLg/llvmpipe environment.

Fresh Windows and Linux ZIP extraction/CRC/checksum checks and native complete-match launches both passed in `reports/evidence/package-after-hours-verification/result.json`. The extracted executable runs exited 0 and completed their matches. The full compatibility audit passed all 51 checks. The completed current-evidence proposal passed the unchanged software gate (76/76); actual outputs are in `reports/evidence/after-hours-rebind/run-20260905T113513-945140Z/`. Final documentation is audited before the helper promotes the ledger and the documentation-only archive repack verifies identical native payloads. Earlier graphics candidate `15a4e880...` and pre-interruption captures remain historical development evidence and do not replace the frozen candidate's final runs.

## Preserved baseline

The prior source candidate is `source-sha256:312471d950685ecc88b89d64a88af35b2b57cabfc1ab9c2b44273191559995fa`, committed as `d1857f5b754fa049ab40588130f7f373f2a35c28`. Byte-identical copies of its candidate, 82-record ledger, actual passing software gate, independent review, documentation audit and evidence index are retained under `reports/evidence/after-hours-baseline/`. `preservation.json` records the copy hashes and time.

That release passed 76/76 software requirements and 3/5 device requirements. Actual local controller hardware, two physical LAN computers and human feel feedback remain external. The old acceptance records, commands, dates, media and runtime hashes retain their original meaning; they are not renamed into new-art evidence.

## Art integration scope

The runtime art inventory contains 19 PNG bitmaps. The new fighter renderer uses explicit image rectangles, local root pivots, common per-board scales, mirror palettes and canonical startup/active/recovery clocks. Both fighters have all 49 canonical move IDs mapped. Rook has 79 selected source cells and 60 state keys; Vale has 80 cells and 65 state keys, for 159 cells and 125 state keys including aliases. The source design contracts reference 36 universal clip intents per fighter. Neither the 125 runtime keys nor the 36 design intents count unique complete animations. Cropped Vale close-normal design cells are excluded from runtime mappings in favor of complete supplemental poses. Both atlases compose phases from selected key poses and shared anticipation/recovery frames; this does not claim newly drawn in-between sequences for every action.

Built-in image generation preserved the original costume and poses while replacing baked checkerboard/gray backgrounds with magenta mattes. Actual PNG inspection showed RGB output, including the failed genuine-alpha attempts. The runtime therefore removes saturated magenta with a shader. The PNGs are not misrepresented as transparent-alpha assets. Prompts, selected-file hashes and preparation notes are stored with each fighter's production assets. Original design boards remain intact.

The shell swaps stage images, fighter sprites, effect sheets, portraits, HUD parts, panels and icons. Canonical numeric content and Core/App sources remain separately protected. The required presentation-source scope is explicit in `tools/verify_after_hours.py`; a filename allowlist is not itself proof that behavior is unchanged. Actual native interface, legal-input showcase, full-match and rendering review runs exercise the changed shell.

## Exact compatibility checks

Run `python tools/verify_after_hours.py --compatibility-only` after the final exports and candidate are produced. This verifies:

- The new source fingerprint includes actual fighter atlases, sprite images, stages, effects and interface assets, and matches the source files on disk.
- Every Core/App source input and all 13 canonical JSON files remain unchanged from the preserved release.
- All four shipped Core/App DLL files match the prior exact hashes on both platforms; the current exports also match the new candidate inventory.
- The inherited executable Core/App/training/balance/network/measurement evidence still has its baseline artifact hashes.
- Atlas references resolve and all 98 move IDs are mapped without changing canonical timings.

The canonical content manifest hash remains `6fd0fe07da1f14d72754869b12cfe6cf21721d5055572c5b9f5b43dd72604c8b`. Core's runtime content hash uses a different algorithm over the same files: `ec249786c06cb4e5cc4a0ea2dd84fe8c818d8360a11977d85f1b9aa66c293e63`. These hashes are not substituted for one another.

Expected frozen module SHA-256 values are Core `204653b031ca2700371d8c1df62c4b7b9eb131825fcaaba1ef5ee01676c2c7df` and App `72e7b96bbd878126db96ba0be8e49bae49df1417bf3162f4aec4d1f325e6cebb`. The first art rebuild added the new Git revision to `AssemblyInformationalVersion`, changing otherwise source-identical module bytes. The integrator reproduced the exact prior DLL hashes with `IncludeSourceRevisionInInformationalVersion=false`. This single build-metadata property is now recorded in `Directory.Build.props` and included in the candidate source fingerprint. The audit explicitly permits only that property and still requires exact shipped module equality; it does not apply a semantic-equivalence waiver or copy different DLLs into an accepted identity. Final exports and quick executable suites must use this corrected configuration.

## New evidence required before rebinding visual claims

The full `python tools/verify_after_hours.py` audit additionally requires the actual new files below. A planned filename or successful import is not counted as a completed run.

| New evidence directory | Required observation |
| --- | --- |
| `reports/evidence/native-after-hours-art/` | Every nonempty canonical move phase and every atlas state selects the expected cell for both mirrored palettes. Actual screenshots support separate visual inspection, with motion evidenced by the separate showcase/match videos; these presentation fixtures are not represented as simulated matches. |
| `reports/evidence/native-after-hours-ui/` | 75 actual native controller-event GUI checks, including completed replay/result/rematch, with clean process exit and decoded video. Software-injected events do not certify physical pads. |
| `reports/evidence/native-after-hours-showcase/` | 13 actual legal-input contact/defense/paid-startup/correction assertions and video, with the new sprites/effects. |
| `reports/evidence/native-after-hours-match/` | A complete Windows native economic match and matching competitive replay, with current-candidate movie and process/runtime hashes. |
| `reports/evidence/native-after-hours-linux/` | A complete Linux native graphical economic match and decoded movie. This PC's WSLg/llvmpipe environment remains explicitly scoped; it is not a second physical computer. |
| `reports/evidence/native-after-hours-pacing/` | A complete normal native match with host metadata and measured frame percentiles, without movie capture. Prior-renderer pacing measurements are historical. |
| `reports/evidence/package-after-hours-verification/` | Fresh Windows/Linux ZIP extraction, CRC/checksum verification and actual native completed matches; extracted native files match the new candidate. |

The audit binds recorded shell DLL, PCK, Core/App and movie hashes to the actual new candidate, including the normal pacing process. Both new full competitive replay files must agree byte for byte and match the completed runtime result. It produces `reports/evidence/after-hours-compatibility/result.json`, with explicit failures while any required evidence or identity check is missing. It does not write the acceptance ledger, waive requirements, or replace model/human visual review. The current source ID is always read from the final candidate manifest; intermediate graphics candidates and their captures are preserved separately if a later presentation fix supersedes them.

After the actual new runs and visual review pass, update the current ledger around this compatibility report, preserve baseline functional artifact references with their original dates, and replace affected visual proof with the new captures. The older free-kit and private-peer films remain useful qualified observations of unchanged gameplay; they do not show the AFTER HOURS artwork. Refresh player documentation and final archive checks, then execute the unchanged software and all-tier gates. The three actual hardware/human requirements remain unverified until performed.

## Controlled ledger update

`python tools/rebind_after_hours_ledger.py --plan` prints the complete mapping for all 82 requirements without writing files or running a gate. Normal invocation first reruns strict compatibility and checks three actual current-candidate reviews: visual findings, an independent substantive review and the final documentation audit. Their defaults are `reports/evidence/after-hours-visual-review.json`, `after-hours-independent-review.json` and `after-hours-documentation-audit.json`. Override them with `--visual-review`, `--independent-review` and `--documentation-review`; the visual option may be repeated. Reviews must identify the current source candidate and successful actual observations. The documentation audit must hash the final document bytes.

The helper verifies the original hashes of every retained baseline artifact. Each attempt gets a distinct timestamped directory under `reports/evidence/after-hours-rebind/`, including its own strict compatibility result, so a failed later attempt cannot overwrite evidence already hashed by an earlier accepted ledger. Its `binding-audit.json` stores the original per-requirement dates, commands, notes and artifact hashes; the new record timestamp describes the current binding audit, not a fictional rerun of old tests. New native UI/showcase/full-match/platform evidence replaces old visual proof. The old free-kit and private-peer films receive an explicit prior-artwork qualification. Exact current DLL/content compatibility is necessary for retaining their gameplay observations.

Normal invocation writes only the attempt's `proposed-ledger.json` and its evidence audit/gate outputs, printing the exact proposal path. It runs the unchanged software gate against that proposal, appends the actual successful preflight to FINAL-001, then checks the finished proposal again. Only explicit `--promote` copies a fully passing proposal to `reports/ACCEPTANCE_RESULTS.json`; it refuses if that file changed concurrently. DEVICE-003, DEVICE-004 and HUMAN-001 stay NOT_RUN. A final all-tier result records those remaining limitations rather than hiding its expected failure.

Finalize actual player instructions, measured limits and status documents before invoking the final proposal/promote sequence. If status documents need a preliminary refresh, their audit must be rerun after their bytes stabilize. Documentation-only archive repacks must separately verify CRC/checksums and exact unchanged native payloads; the earlier successful fresh native executions are retained with their original ZIP hashes.

## Desktop interaction and final delivery

The final Windows executable was also opened through actual desktop app control. Keyboard navigation reached the Training lineup and arena, and F1 paused the lab. Four F12 native viewport captures and the direct observations are preserved in `reports/evidence/after-hours-manual/`. A separate model inspected all 14 legal-input showcase screenshots; the integrator directly inspected corresponding native Windows/Linux combat images. These observations supplement the native assertions and do not claim physical gamepad testing or human acceptance.

The two platform ZIPs include the current bitmap renderer, player instructions, provenance, runtime dependencies and SHA-256 inventory. `tools/verify_repacked_archives.py --prior-label package-after-hours-verification --label after-hours-final-archive-integrity` verifies the final documentation repack against the actual fresh-extraction launches without inventing another gameplay run.
