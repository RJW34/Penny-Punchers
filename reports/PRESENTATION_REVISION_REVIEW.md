# Presentation revision review

Reviewed candidate `312471d950685ecc88b89d64a88af35b2b57cabfc1ab9c2b44273191559995fa` on 2026-09-05. The comparison inventory is `evidence/candidate-compatibility/result.json`; it establishes byte-identical Core/App assemblies and canonical content between this candidate and the previous exported recordings. The prior PCK contains stripped C# stubs, so this review does not claim a recovered complete textual diff. I reviewed the current changed methods and the edits performed in this task.

`ArenaView` owns a 1280×720 `SubViewport` that updates once. `FoundryBackdrop` contains the original foundry drawing primitives with fixed cosmetic camera/time values. The arena draws the resulting texture as one quad and retains live fighter poses, projectiles, impact effects, training grid and world-positioned corner pylons. The viewport is a scene-owned child and its cached texture wrapper is disposed at exit. There are no Core references or simulation writes in the backdrop. This removes repeated decorative draw submission while preserving competitive geometry and inputs.

The only authored normal-menu layout change moves the selection screen navigation hint beneath the CPU-selector row. The new controller-flow assertions exercise this separation, generate a complete match through production bot inputs, independently verify the replay, open the actual results screen and activate its existing rematch callback through a software joypad event. The additional fixture runs only under the explicit UI-test flag; it does not persist player settings. The normal rematch implementation remains the existing `StartMatch` path.

The remaining changed shell methods contain the opt-in timing scopes, diagnostic output at orderly quit and the explicit no-screenshots capture switch. `RuntimeProfiler.Measure` returns an inert scope without the `--profile` flag; its enabled path reads elapsed time and accumulates diagnostic samples. These paths neither produce combat inputs nor debit wallets. `Main.Rendering.UpdateArena` continues to read simulation state into presentation DTOs.

Fresh exported validation covers the changed shell and renderer: `evidence/native-ui-final` has 75 passing assertions, 11 valid PNGs, a fully decoded movie and a clean exit. It verifies result → controller rematch → fresh 600 CR wallets, zero scores/leases and round 1. `evidence/native-showcase-final` has 13 passing actual-input training assertions, 14 valid PNGs, a fully decoded movie and a clean exit. Selected images show EX startup spending, separately timed super parries and the corrected rollback-training branch. `evidence/native-match-final` completed all six competitive rounds with the previous verified final hash, a clean exit, 54 valid PNGs and a fully decoded 229-second movie. Its result reads Vale 5 to 1 with 1200 CR each, matching the earlier Windows and Linux runs.

The original Windows/free-kit/network/Linux movies retain their original runtime hashes. They remain exact gameplay evidence for unchanged Core/App binaries, and are explicitly earlier presentation captures. Software injected pads do not establish physical controller or unplug behavior; movie capture timing does not establish ordinary rendering performance.
